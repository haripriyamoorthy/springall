#datasource specific properties

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/upworkdb
#jpa properties

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true