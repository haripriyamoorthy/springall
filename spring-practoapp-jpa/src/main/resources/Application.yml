server:
  port: 8081
jwt:
  secret: shristi   
#datasource specific properties
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/practodb
#jpa properties

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true